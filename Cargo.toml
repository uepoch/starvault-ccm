[workspace]
resolver = "2"
members = ["crates/core", "crates/app"]

[workspace.package]
version = "0.1.6"
edition = "2021"
license = "MIT"
authors = ["StarVault CCM contributors"]

[workspace.dependencies]
thiserror = "2"

# M1 dependencies (added as modules land):
# sha2, hex, rusqlite (bundled), serde, toml, walkdir, rayon
